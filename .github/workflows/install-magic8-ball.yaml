name: Install ✨ 🎱
on:
  push:
    branches:
      - "**"

jobs:
  magic8ball:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Invoke Magic 8 Ball
        uses: phil-loops/LoopsMagic8Ball@main
