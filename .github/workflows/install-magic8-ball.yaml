name: Install ✨ 🎱
on:
  push:
    branches:
      - "**"

jobs:
  magic8ball:
    runs-on: ubuntu-latest
    steps:
      - name: Invoke the Magic 8 Ball
        uses: phil-loops/LoopsMagic8Ball/.github/workflows/magic8ball.yaml@main
